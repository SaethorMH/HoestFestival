---
---
<header>
   

    
  <a href="/"><h1>Høst Festival</h1></a>
  <button>Køb billetter her!</button>

</header>
  <style>
    header {
        min-height: 100px;
        background-color: var(--primary);
        color: var(--buttonColor);
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 1rem;
    }

    button {
        background-color: var(--accent);
        color: var(--buttonTextColor);
        border: none;
        padding: 0.5rem 1rem;
        cursor: pointer;
        margin-top: 1rem;
    }
    a{
      text-decoration: none;
      color: var(--black);
    }
  </style>