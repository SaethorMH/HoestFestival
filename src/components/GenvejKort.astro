---
const {title, text, link, icon} = Astro.props
---

<article>
    <h3>{title}</h3>
    <img src={icon} alt="genvejIcon">
    <p>{text}</p>
    <a href={link}>Se mere her!</a>

</article>

<style>
    article{
        display: grid;
        background-color: var(--primary);
    }
</style>